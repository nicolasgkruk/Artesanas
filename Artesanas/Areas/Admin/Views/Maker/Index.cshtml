@model IEnumerable<Tipo>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br /><br />
      <div class="backgroundWhite">
          <div class="row">
              <div class="col-md-6">
                  <h2 class="text-info">Empresas productoras de cerveza artesana</h2>
              </div>

              <div class="col-md-6 text-right">
                  <partial name="_CreateButtonPartial" />
              </div>
          </div>
          <br />

          @if (Model.Count() > 0)
          {
          <table class="table table-striped border">
              <tr class="class-secondary">
                  <th>
                      @Html.DisplayNameFor(x => x.Name)
                  </th>
                  <th></th>
              </tr>
              @foreach (var tipo in Model)
              {
                  <tr>
                      <td>
                          @Html.DisplayFor(model => tipo.Name)
                      </td>                    
                      <partial name="_TableButtonPartial" model="tipo.Id" />
                  </tr>
              }
          </table>
          }
          else
          {
              <p>No existe ninguna empresa productora de cerveza artesana cargada en la base de datos.</p>
          }

      </div>
